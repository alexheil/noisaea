<%= provide(:title, "Resend Unlock Instructions | Noisaea //") %>
<%= render 'partials/uni_header' %>
<div class="buffalo">
  <div class="delaware">
    <div class="elmwood">
      <div class="harlem">
        <h1>resend unlock instructions</h1>
      </div>
      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>

        <div class="highgate">
          <div class="amherst temp">
            email
          </div>
          <%= f.email_field :email, autofocus: true, class: "huron" %>
        </div>

        <div class="ellicott">
          <%= f.submit "resend unlock instructions", class: "exchange" %>
        </div>
      <% end %>
      <div class="virginia">
        <%= render "producers/shared/links" %>
      </div>
    </div>
  </div>
</div>
<%= render 'partials/uni_footer' %>
