<%= provide(:title, "Add A Video") %>
<div class="buffalo">
  <div class="delaware">
    <div class="elmwood">
      <div class="harlem">
        <h1>add a video</h1>
      </div>

      <%= form_for(@video, url: record_label_videos_path) do |f| %>
        <%= f.hidden_field :record_label_id %>
        <%= render 'errors/video_errors' %>
        <div class="highgate">
          <div class="amherst temp">
            title
          </div>
          <%= f.text_field :title, class: "huron" %>
        </div>
        <div class="highgate">
          <div class="amherst temp">
            youtube url
          </div>
          <%= f.text_field :youtube_url, class: "huron" %>
        </div>
        <div class="highgate">
          <div class="amherst temp">
            description
          </div>
          <%= f.text_area :description, class: "chippewa" %>
        </div>
        <div class="ellicott">
          <%= f.submit "add video", data: { disable_with: 'posting...' }, class: "exchange" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
<%= render 'partials/uni_footer' %>
