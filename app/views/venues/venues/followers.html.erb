<%= provide(:title, sanitize(@venue.venue_name) + " | Followers") %>
<div class="buffalo">

  <%= render 'venues/partials/venue_cover_image' %>

  <div class="delaware">

    <%= render 'venues/partials/venue_name' %>

    <% if @venue.fans.any? %>
    <div class="harlem">
        <% @venue.fans.take(100).each do |fan| %>
          <div class="utica">

            <%= render 'fans/partials/fan_profile_image', :fan => fan %>

            <div class="busti">
              <%= link_to fan.fan_name, fan_path(fan) %>
            </div>
          </div>
        <% end %>
    </div>
    <% end %>

  </div>
</div>
<%= render 'partials/uni_footer' %>
