<%= provide(:title, sanitize(@artist.artist_name) + " | Followers") %>
<div class="buffalo">

  <%= render 'artists/partials/artist_cover_image' %>

  <div class="delaware">

    <%= render 'artists/partials/artist_name' %>

    <% if @artist.fans.any? %>
    <div class="harlem">
      <% @artist.fans.take(100).each do |fan| %>
        <div class="utica">

          <%= render 'fans/partials/fan_profile_image', :fan => fan %>

          <div class="busti">
            <%= link_to fan.fan_name, fan_path(fan) %>
          </div>
        </div>
      <% end %>
    </div>
    <% end %>

  </div>
</div>
<%= render 'partials/uni_footer' %>
